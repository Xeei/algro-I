#include <stdio.h>

int main()
{

    //? basic
    // int arr[400];
    // arr[0] = 1;
    // arr[399] = 2;

    // int var1 = arr[0];

    //? display address (%p)
    // printf("arr[1]: %p\n", &arr[1]);
    // printf("var1: %p\n", &var1);

    //? display with llop
    // int arr[5] = {10, 20, 30, 40, 50};

    // for (int i = 0; i < 5; i++)
    // {
    //     printf("%d:%d\n", i, arr[i]);
    // }

    return 0;
}